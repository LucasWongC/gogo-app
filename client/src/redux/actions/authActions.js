import axios from "axios";

export const newRegistration = (payload) => async (dispatch) => {
  /*   try {
        const config = {
            method: 'POST',
            headers: { 
                "Content-Type": "application/json"
            },
            body:JSON.stringify(payload)
        }
       // console.log(payload);
     
        const register = await fetch('http://localhost:7000/api/auth/register-user',config)

        console.log(await register.json());
    } catch (error) {
        console.log(error);
         
    }
     */

  /*   const config = {
        headers: {
          "Content-Type": "application/json",
        },
      };
    
      const body = JSON.stringify(payload);
      try {
        const res = await axios.post("http://localhost:7000/api/auth/register-user", body, config);
    } catch (err) {
        console.log(err.response.data);
        
    } */
}